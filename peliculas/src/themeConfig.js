// themeConfig.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    ulima: {
      main: 'rgb(250, 117, 37)', // Configura el color naranja con el código RGB
    },
  },
});

export default theme;
